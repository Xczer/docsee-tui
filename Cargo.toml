[package]
name = "docsee"
version = "0.1.0"
edition = "2021"
description = "A terminal UI for managing Docker containers, images, volumes and networks"
authors = ["Xczer <xczermax@gmail.com>"]
license = "MIT"
repository = "https://github.com/Xczer/docsee-tui"

[dependencies]
# TUI framework
ratatui = "0.29.0"
crossterm = { version = "0.28", features = ["event-stream"] }

# Docker client
bollard = { version = "0.18.1", features = ["ssl"] }

# Async runtime
tokio = { version = "1.43.0", features = ["full"] }

# Error handling
color-eyre = "0.6.3"
anyhow = "1.0"

# CLI parsing
clap = { version = "4.5.31", features = ["derive"] }

# Utilities
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
dirs = "5"
chrono = { version = "0.4", features = ["serde"] }
byte-unit = { version = "5.1", features = ["byte"] }

# for log streaming
tokio-stream = "0.1"
futures = "0.3"

[dev-dependencies]
tokio-test = "0.4"
